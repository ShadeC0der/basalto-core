[package]
name = "basalto-core"
version = "0.3.0"
edition = "2024"

[dependencies]
basalto-shared = { path = "../basalto-shared/" }
libloading = "0.9.0"
serde = { version = "1", features = ["derive"] }
toml = "1"
